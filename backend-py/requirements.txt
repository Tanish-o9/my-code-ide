# ---- Core ----
Django==5.0.6
djangorestframework==3.15.1
django-cors-headers==4.3.1
django-filter==24.2
dj-database-url==2.1.0
psycopg[binary]==3.1.19
python-dotenv==1.0.1

# ---- Auth / JWT ----
djangorestframework-simplejwt==5.3.1
PyJWT==2.8.0

# ---- Enterprise SSO ----
python3-saml==1.16.0          # SAML 2.0 SP (xmlsec-backed assertion validation)
Authlib==1.3.0                # generic OAuth2 / OIDC (Okta, Azure AD, Google Workspace)
requests==2.32.3

# ---- Async jobs ----
celery==5.4.0
redis==5.0.4
django-celery-beat==2.6.0     # periodic: retention purge, grace-period reaper

# ---- Storage / export packaging ----
boto3==1.34.113               # signed, expiring S3 download links

# ---- Dev / test ----
pytest==8.2.1
pytest-django==4.8.0
